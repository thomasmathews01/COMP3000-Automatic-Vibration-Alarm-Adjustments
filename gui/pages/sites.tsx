import React from 'react';
import Container from '@material-ui/core/Container';

export default function Sites() {

    return (
        <Container component="main" maxWidth="xs">
            <p> You have reached what will be the sites page.</p>
        </Container>
    );
}

//TODO: Replace icon.