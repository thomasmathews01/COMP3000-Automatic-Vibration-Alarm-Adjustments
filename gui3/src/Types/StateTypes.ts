export interface State {
    id: number,
    name: string
}

export interface StateValue {
    id: number,
    value: number
}

export interface statePeriod {
    id: number,
    start: number,
    end: number
}